# PNN - Lip-based Speaker Recognition

## Generazione del CSV

- Per ogni persona (identità unica), sono presenti *tre video*
- Per ogni video, sono estratti gli ultimi *tre frame*
- Per ogni frame, viene generata *una riga* in `dataset.csv`